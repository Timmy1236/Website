---
permalink: /latest/en.json
eleventyExcludeFromCollections: true
---

{
  "blog": {
    "title": "{{ collections.blog_en[0].data.title }}",
    "url": "{{ collections.blog_en[0].url }}",
    "date": "{{ collections.blog_en[0].date | readableDate }}",
    "description": "{{ collections.blog_en[0].data.description }}",
    "preview": "{{ collections.blog_en[0].data.preview }}"
  },
  "changelog": {
    "title": "{{ collections.changelog_en[0].data.title }}",
    "url": "{{ collections.changelog_en[0].url }}",
    "date": "{{ collections.changelog_en[0].date | readableDate }}",
    "description": "{{ collections.changelog_en[0].data.description }}",
    "preview": "{{ collections.changelog_en[0].data.preview }}"
  }
}