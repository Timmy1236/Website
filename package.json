{
  "name": "timmys-digital-dumpster",
  "version": "1.0.0",
  "description": "Una simple página web que contendrá información básica de mí y de todos los proyectos que haré en algún futuro.",
  "browserslist": [ ">0.3%, defaults and fully supports es6-module and baseline widely available" ],
  "scripts": {
    "clean": "node -e \"fs.rmSync('public/dist', { recursive: true, force: true })\"",
    "prebuild": "pnpm clean",
    "build": "concurrently \"npm:eleventy\" \"npm:js\" \"npm:css\" \"npm:css:pages\"",
    "dev": "concurrently \"npm:eleventy:watch\" \"npm:js:watch\" \"npm:css:watch\" \"npm:css:pages:watch\"",
    "lint": "pnpm eslint && pnpm stylelint",
    "eslint": "pnpm exec eslint .",
    "stylelint": "pnpm exec stylelint \"**/*.css\"",
    "eleventy": "pnpm exec eleventy",
    "eleventy:watch": "pnpm exec eleventy --watch",
    "js": "esbuild src/app/app.ts --bundle --minify --format=esm --outfile=public/dist/main/app.js",
    "js:watch": "esbuild src/app/app.ts --bundle --minify --format=esm --watch --sourcemap --outfile=public/dist/main/app.js",
    "css": "esbuild src/styles/main/main.css --bundle --minify --external:/assets/* --outfile=public/dist/main/styles.css",
    "css:watch": "esbuild src/styles/main/main.css --bundle --minify --watch --external:/assets/* --outfile=public/dist/main/styles.css",
    "css:pages": "esbuild src/styles/data/main.css --bundle --minify --external:/assets/* --outfile=public/dist/pages.css",
    "css:pages:watch": "esbuild src/styles/data/main.css --bundle --minify --watch --external:/assets/*  --outfile=public/dist/pages.css",
    "purgecss": "node scripts/purgecss-report.mjs",
    "typecheck": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Timmy1236/Website.git"
  },
  "keywords": [
    "website",
    "web"
  ],
  "author": "Timmy1236 (https://timmy.nekoweb.org/)",
  "licenses": [
    {
      "type": "CC BY-NC-SA 4.0",
      "url": "https://github.com/Timmy1236/Website/blob/main/LICENSE"
    }
  ],
  "type": "module",
  "bugs": {
    "url": "https://github.com/Timmy1236/Website/issues"
  },
  "homepage": "https://github.com/Timmy1236/Website#readme",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@eslint/js": "^10.0.1",
    "@types/mithril": "^2.2.8",
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "eslint": "^10.6.0",
    "eslint-plugin-compat": "^7.0.2",
    "purgecss": "^8.0.0",
    "stylelint": "^17.14.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-no-unsupported-browser-features": "^8.1.1",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.63.0"
  },
  "dependencies": {
    "mithril": "^2.3.8"
  }
}
